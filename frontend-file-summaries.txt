{
  "frontend/src/App.tsx": "Root application component with a single `popstate` listener for browser navigation, a `handleAdminLoginSuccess` callback passed to `ScratchPage` that directly sets auth state and navigates to AdminPanel, and a `handleAdminLogout` that clears sessionStorage and returns to ScratchPage.",
  "frontend/src/utils/rewardGenerator.ts": "Exports a `RewardResult` discriminated union type and `generateReward()` with updated probabilities: 40% ₹10, 40% better_luck, 18% ₹20, ~1.99% ₹30, 0.01% ₹100. Also exports the unchanged `generateUniqueCode()` function.",
  "frontend/src/utils/audioGenerator.ts": "Web Audio API utility with playScratchSound() (white noise burst) and playWinningChime() (ascending C-E-G-C tone sequence) functions.",
  "frontend/src/hooks/useScratchCanvas.ts": "Custom React hook managing canvas-based scratch interaction with pointer/touch events, pixel erasure, progress tracking, and reveal threshold detection.",
  "frontend/src/components/WaterDropIntro.tsx": "Canvas-based water drop intro animation component rendering 20 falling drops over 2.2 seconds with a gradient background, then calling onComplete.",
  "frontend/src/components/Confetti.tsx": "Canvas-based confetti burst component with 120 blue/white/gold particles using requestAnimationFrame physics, triggered once on reveal and lasting ~3.5 seconds.",
  "frontend/src/components/ScratchCard.tsx": "Glassmorphism scratch card accepting a `RewardResult` prop; conditionally renders either the monetary reward amount (with gold styling) or a \"Better Luck Next Time!\" consolation message (with clover emoji and blue styling) while always displaying the unique coupon code.",
  "frontend/src/components/Logo.tsx": "Vitalis Water logo header component with explicit logo-image CSS class for full visibility, optional onDoubleClick prop for admin modal trigger, and fallback emoji display.",
  "frontend/src/components/Hero.tsx": "Hero section with animated \"Scan. Scratch. Win.\" tagline, floating bottle image, and subtitle.",
  "frontend/src/components/RewardReveal.tsx": "Glassmorphism reward reveal card showing the won amount, description, validity notice, and a gold \"Fill Claim Form Below\" button that scrolls to the claim form.",
  "frontend/src/components/Footer.tsx": "Site footer with Vitalis logo, copyright, tagline, and caffeine.ai attribution — the admin panel link has been removed entirely so no visible admin access hint is shown to regular users.",
  "frontend/src/pages/ScratchPage.tsx": "Main scratch card page that stores a `RewardResult` discriminated union in state, only triggers confetti and renders `RewardReveal`/`ClaimForm` when `rewardResult.type === 'reward'`, and passes the full `rewardResult` to `ScratchCard`.",
  "frontend/src/index.css": "Complete Vitalis Water design system with all existing styles plus new `.scratch-card-badge-luck`, `.scratch-card-luck-section`, `.scratch-card-luck-emoji`, `.scratch-card-luck-title`, `.scratch-card-luck-subtitle`, and `.scratch-card-luck-msg` classes for the \"Better Luck Next Time\" card variant.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Vitalis brand color tokens, Poppins/Inter font families, glass/glow shadow utilities, and animation keyframes for float, shimmer, pop, ripple, coin-bounce, and fade-in-slide-up.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — fixes `useGetSubmissionByCode` to use `searchByCouponPrefix` with exact-match filtering instead of the non-existent `getSubmissionByCode` method, removes the redundant `ensureAdminActor` helper (admin auth is already handled in `useActor.ts`), and preserves all other query/mutation hooks with proper error normalization and cache invalidation.",
  "frontend/src/components/ClaimForm.tsx": "Glassmorphism claim form with State dropdown, City, UPI ID, and Feedback fields — now calls `submitClaim.reset()` before each retry to clear stale error state, shows a clean user-friendly error message inline (without raw IC error codes), keeps the Submit button enabled after an error (showing \"🔄 Retry Submission\" label), and only disables the button while actively pending.",
  "frontend/src/components/ConfirmationNotification.tsx": "Full-screen overlay confirmation notification with animated checkmark, ripple rings, bouncing coins, \"Money will be in your account soon!\" message, and auto-dismiss after 6 seconds.",
  "frontend/src/pages/AdminPanel.tsx": "Admin dashboard with stats row, CouponVerifyPanel (using `useGetSubmissionByCode` hook), status filter tabs, searchable submissions table, Refresh and Logout buttons — fixes the `onClick={refetch}` type error by wrapping in arrow functions, and properly surfaces loading/error states with a Retry button.",
  "frontend/src/components/AdminLoginModal.tsx": "New glassmorphism-styled modal dialog using shadcn/ui Dialog, Input, Label, and Button components that validates hardcoded admin credentials (email: shajan@vitalis.com, password: India@123) and calls `onSuccess` on match or shows an inline error on failure."
}